import React from 'react';
import { Target } from 'lucide-react';
import Component from './index';

export const config = {
  id: 'chess',
  title: 'Chess',
  icon: <Target size={32} color="#444" />,
  component: <Component />
};
