import React from 'react';
import { Settings } from 'lucide-react';
import Component from './index';

export const config = {
  id: 'settings',
  title: 'Settings',
  icon: <Settings size={32} color="#757575" />,
  component: <Component />
};
