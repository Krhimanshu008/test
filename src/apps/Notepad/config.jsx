import React from 'react';
import { Pencil } from 'lucide-react';
import Component from './index';

export const config = {
  id: 'notepad',
  title: 'Notepad',
  icon: <Pencil size={32} color="#febb08" />,
  component: <Component />
};
